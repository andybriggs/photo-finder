# Flickr Photo Finder

## Setup

`git clone git@bitbucket.org:abriggs/photofinder.git`

`cd photofinder`

`npm i`

## Run locally

`npm start`

## Run tests

`npm test`

## Create distribution bundle

`npm run build`
