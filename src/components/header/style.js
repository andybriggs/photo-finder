import { StyleSheet } from 'aphrodite/no-important';

const styles = StyleSheet.create({
  header: {
    padding: '0 20px',
    textAlign: 'center',
    color: '#fff'
  },
  title: {
      color: '#fab630'
  }
});

export default styles;
